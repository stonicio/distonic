---
build:
- name: build container
  docker_build: null
test:
- name: run tests
  docker_run:
    cmd: go test
release:
- name: publish container
  docker_registry:
    repo: stonicio/distonic
    tags:
      - '{{ .BranchDashed }}'
...